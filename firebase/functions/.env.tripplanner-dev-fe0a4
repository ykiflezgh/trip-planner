# Dev project only: push to the actor's own devices too, so a single test account can observe
# the fan-out (design §10 excludes the actor). Do not create this for the prod project.
NOTIFY_ACTOR_DEBUG=true

# Design §8.7 kill switch (Remote Config `suggest_order_enabled` later). Set it in every project's .env, or non-interactive deploys prompt for it.
SUGGEST_ORDER_ENABLED=true

# Anthropic workspace to bill (Console > Settings > Workspaces, `wrkspc_…`) when the API key is organization-level;
# leave empty for a workspace-scoped key. Every project's .env needs the line or non-interactive deploys prompt.
ANTHROPIC_WORKSPACE_ID=wrkspc_019YY2d5PQrKyna2c57kGDDg
